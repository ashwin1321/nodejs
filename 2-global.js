// GLOBAL VARIABLES

// __dirnmae    - the name of the current directory
// __filename   - the name of the current file
// require      - a function that can be used to load other modules (CommoonJS)
// module       - info about the current module (file)
// process      - info about the env where the program is runnings

console.log(__dirname);

setInterval(() => {
    console.log("hello world");
}, 1000);

// setTimeout(() => {
//     console.log("bye world");
// }, 2000);
